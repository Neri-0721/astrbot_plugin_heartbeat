{
  "interval_minutes": {
    "title": "心跳预检间隔",
    "description": "每隔多少分钟检查一次条件。条件不满足时自动跳过，不消耗 token",
    "type": "int",
    "default": 90,
    "minimum": 10,
    "maximum": 1440
  },
  "startup_behavior": {
    "title": "启动行为",
    "description": "on_interaction = 首次对话后才启动心跳; auto = 插件加载后立即启动（需至少有一次对话记录）",
    "type": "string",
    "options": ["on_interaction", "auto"],
    "default": "on_interaction"
  },
  "target_mode": {
    "title": "目标模式",
    "description": "all = 遍历所有会话，符合条件的触发; last = 只对最后聊天的会话生效",
    "type": "string",
    "options": ["all", "last"],
    "default": "all"
  },
  "active_hours_start": {
    "title": "活跃时段（开始）",
    "type": "string",
    "default": "08:00",
    "hint": "HH:MM 格式"
  },
  "active_hours_end": {
    "title": "活跃时段（结束）",
    "type": "string",
    "default": "23:30",
    "hint": "HH:MM 格式"
  },
  "check_last_interaction": {
    "title": "检测互动间隔",
    "description": "启用后若近期有对话则跳过，免打扰",
    "type": "bool",
    "default": true
  },
  "interaction_threshold_minutes": {
    "title": "互动静默阈值",
    "description": "距上次对话不足此分钟数时跳过",
    "type": "int",
    "default": 30,
    "minimum": 5,
    "maximum": 1440
  },
  "max_daily_heartbeats": {
    "title": "每日唤醒上限",
    "description": "每天最多唤醒 Agent 的次数，控制 API 费用",
    "type": "int",
    "default": 48,
    "minimum": 1,
    "maximum": 288
  },
  "cooldown_seconds": {
    "title": "冷却间隔（秒）",
    "description": "两次唤醒之间的最小间隔",
    "type": "int",
    "default": 120,
    "minimum": 10,
    "maximum": 3600
  },
  "heartbeat_content": {
    "title": "HEARTBEAT.md 内容",
    "description": "在此编辑心跳任务内容。点保存后自动同步到 HEARTBEAT.md 文件。留空则恢复文件原有内容。使用自然语言描述即可。",
    "type": "string",
    "default": "",
    "hint": "编辑后保存，内容将写入 HEARTBEAT.md",
    "is_long_text": true
  },
  "heartbeat_file_path": {
    "title": "自定义 HEARTBEAT.md 路径（可选）",
    "description": "指向自定义文件路径。设置后 heartbeat_content 和 per-session 模式失效。",
    "type": "string",
    "default": "",
    "hint": "留空=默认; 或填写绝对路径"
  }
}
