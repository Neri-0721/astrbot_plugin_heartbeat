# HEARTBEAT.md — 心跳任务定义

本文件由心跳插件在每次唤醒时读取。根据定义的规则执行任务。

---

## 规则说明

- 每行非空、非注释（`#` 开头）的文本都被视为一个任务
- 任务格式：`<检查项>: <阈值> => <消息>`
- 当前支持的检查项见下方示例

---

## 内置检查项（由插件自动处理，无需写在此文件）

- ✅ 活跃时段检查（配置中定义）
- ✅ 互动间隔检查（配置中定义）
- ✅ 夜间模式催休息（配置中定义）

---

## 自定义任务（可在此文件自由添加）

### 示例：定期问候
```
time_since_last_interaction > 120 => 检测到已长时间没有对话，来看看你有没有好好照顾自己。
```

### 示例：整点报时
```
cron: 0 9 * * 1-5 => 早上好，今天是工作日。记得吃早餐。
```

### 示例：时间敏感提醒
```
hour == 12 => 中午了，该吃饭了。
hour == 18 => 晚饭时间到，别饿着。
hour == 23 => 深夜了，希望今天的任务都完成了。请好好休息。
```

---

## 任务格式说明

| 格式 | 说明 | 示例 |
|------|------|------|
| `hour == N` | 当前小时等于N时触发 | `hour == 12 => 中午好` |
| `time_since_last_interaction > N` | 距上次对话超过N分钟 | `time_since_last_interaction > 120 => 好久不见` |
| `cron: <表达式>` | 按cron表达式定时触发 | `cron: 0 9 * * 1-5 => 工作日早安` |

---

## 配置提示

在 astrbot 管理面板 → 插件配置 → Heartbeat 中可调整：
- 心跳间隔（默认90分钟）
- 目标用户/群聊
- 活跃时段
- 互动检测灵敏度
- 夜间模式时间
- 是否启用此任务文件
